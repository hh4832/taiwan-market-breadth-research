修改路徑
PROJECT_DIR = Path.cwd()

CACHE_DIR = PROJECT_DIR / "cache"
OUTPUT_DIR = PROJECT_DIR / "output" / "market_breadth_0050_study"

CACHE_DIR.mkdir(parents=True, exist_ok=True)
OUTPUT_DIR.mkdir(parents=True, exist_ok=True)

需要修正總家數
=======================================================

v2 更新內容
修正市場篩選邏輯，正確納入 上市（sii） 與 上櫃（otc） 普通股，排除興櫃（rotc）。
修正市場廣度快取（cache）機制，避免修改股票母體後仍誤讀舊版市場廣度資料。
新增市場篩選檢查（Market Filter Audit），輸出各市場股票數與實際納入數量，方便驗證研究母體是否正確。
強化資料驗證流程，修正市場廣度家數（up/down/flat/valid count）與人工驗證的一致性檢查。

=======================================================

v3：新增 Excess Return vs. Unconditional Figure，顯示各市場廣度分組相對於全樣本平均報酬的超額報酬。

=======================================================

v4 主要更新：
新增 up_ratio = up_count / (up_count + down_count)
新增 down_ratio = down_count / (up_count + down_count)
兩者沿用原本完整流程：
Q1–Q5
LOW_5／HIGH_5
HAC 檢定
Sharpe、Cohen’s d
圖形
Excel／CSV／Parquet 輸出
新增比例代數驗證：
up_ratio + down_ratio = 1
breadth_net_ratio = 2 × up_ratio − 1
breadth_net_ratio = 1 − 2 × down_ratio
Excel 新增：
predictor_pearson
predictor_spearman
市場廣度快取改為 v4 專用檔名，若舊快取缺少新 predictor，程式也會自動重建，不會誤讀 v3 結果。
已清除 notebook 舊執行輸出，並通過全部 code cells 的靜態語法檢查。

=======================================================

v5 新增內容：

calc_hac_mean_vs_zero()：以只有截距的 OLS＋HAC 檢驗 E(return | group) = 0。
calc_binomial_test()：雙尾檢驗訊號組勝率是否不同於 50%。
apply_multiple_testing_correction()：分別對 HAC mean-vs-zero 與 binomial p-value 執行 BH-FDR、Bonferroni。
run_mean_vs_zero_analysis()：沿用原本 predictor × target × group 流程執行新分析。
merge_all_results_v5()：將新統計欄位合併到原 all_results，產生 all_results_v5。
新增 HAC mean、HAC standard error、t-value、p-value、95% CI、勝率相對 50%、binomial p-value 與兩套多重比較欄位。
新增 Mean Return vs Zero 圖，使用原始 group mean、HAC 95% CI、零報酬線與樣本數。
Excel 新增：
mean_vs_zero_results
all_results_v5
保留原本 all_results 的欄位與用途，不以新檢定覆蓋 Group vs Non-group 結果。
新增相關 validation，檢查 HAC intercept、CI、勝率公式、成功／失敗筆數、修正後 p-value、merge 完整性與無限值。

多重比較的檢定家族分別定義為全部有效的：

predictor × target × group_type × group

HAC mean-vs-zero 與 binomial test 各自獨立修正，不混成同一組 p-value。

=======================================================

v6：Breadth Dynamics & Extreme Breadth Study

- 保留 v5 notebook 作為不可覆寫的 baseline，核心 v6 邏輯移至 src/market_breadth/。
- 正式 predictor：up/down level、3 日 persistence、1 日 delta、1 日 acceleration、±5% extreme、limit-up/down ratio。
- 新增 trailing 252 日 PR 與 Z；正式訊號只使用 t 與過去資料，不使用 full-sample rank。
- 新增 0050 Close[t] vs MA60[t] 的 ALL/BULL/BEAR 分層。
- 主要 targets 限定 ret_c0_o1、ret_c0_c1、ret_o1_c1、ret_o1_o2、ret_o1_c2、ret_o1_c3。
- 分開計算 group vs non-group、group vs 同 regime unconditional mean、group mean vs zero。
- 四類假說分別執行 global 與 research-family BH-FDR/Bonferroni。
- 新增尾端風險、predictor correlation、PR quintile monotonicity、年度穩健性及 concentration warning。
- 漲跌停價不再使用 ±9.5% 報酬近似，改由參考價、歷史漲跌幅制度與 tick size 計算。
- 新增 Colab runner、Google Drive timestamped archive、合成資料 unit/smoke tests。
- 漲跌停判斷改採前一個有效收盤價＋臺股 tick-size rounding；2015-06-01 前使用 7%，之後使用 10%，並支援公司行動參考價覆蓋。
- README 新增可直接開啟 main branch v6 notebook 的 Open in Colab badge。

=======================================================

v11：Breadth Mechanism & Regime Reconciliation

- 固定 v10 Candidate A/B/C，不新增 predictor、PR window、holding horizon 或最佳化 threshold。
- 使用 adjusted 0050 close 建立 prior 1/3/5/10 日報酬，分別控制後估計 breadth beta、HAC CI 與 attenuation。
- 將 up_ratio PR95 與 big_up_ratio 5D PR60/80 映射為 strict t-1 historical raw threshold及年度漂移。
- 以歷史正確 ±7%／±10% limit rule 比較制度切換前後，並正式估計 Signal×Post2015 interaction。
- 新增三模組獨立輸出、mechanism summary、13 個 Excel sheets、8 張圖與 stage/progress logging。

=======================================================

v10：Post-2015 Breadth State & PR-Bin Validation

- 新增 participation breadth，正式比較 participation、extreme ±5% 與 limit breadth。
- 六個 raw ratio 建立 1/3/5 日 trailing mean 與 Level／Delta；不加入 acceleration。
- PR60／126／252 採 t-1 以前 strict historical reference，primary inference 改為七個互斥且完備的 PR bins。
- Primary comparison 為 bin vs non-bin，平均報酬與勝率分開作 family/global FDR 與 Bonferroni。
- 加入 ordered-bin shape、Spearman、high-low contrasts、non-overlap、年度穩健性與 small-N guardrails。
- 新增 v10 Excel/Parquet、10 類圖形、validation summary 與可重複 Run All 的 Colab runner。

=======================================================

v9：Post-2015 Breadth PR & Win-Rate Validation

- Primary sample 固定自 2015-06-01 起，避免混合 7% 與 10% 漲跌停制度。
- 只使用 LIMIT、EXTREME_5PCT 與其 1/3/5 日平均後的一階 DELTA；不使用 acceleration 或 Z-score。
- rolling PR 使用 60/126/252 日 t-1 歷史分布，明確排除 t。
- 0050 O1→C1/C3/C5/C10/C20 outcome 強制使用 etl:adj_open / etl:adj_close。
- 新增 signal-vs-non-signal LPM HAC 勝率推論，平均報酬與勝率分開作 family/global FDR。
- 新增 exact-mask 去重、descriptive PR bins、年度與 non-overlapping robustness。
- local/Drive output 改用單一 timestamp、version 與 commit 組成的 run_id，拒絕覆寫。
- 新增可重複 Run All 的 v9 Colab runner與 synthetic tests。
- Drive root 確認為 `/content/drive/MyDrive/Quant_Research/taiwan-market-breadth-research`，不存在時停止且不得自動建立。
- Python 非 3.11 時改為 warning，安裝依賴、匯入套件並通過 unit tests 後才允許繼續，run_info 記錄 runtime deviation。

=======================================================

v7：Robustness Validation

- 以實際訊號日期遮罩 hash 去除 PR 別名與完全相同鏡像假設後，重新計算 FDR/Bonferroni。
- 新增預先指定候選的年度穩定性、leave-one-year-out 與五分位 HAC 趨勢檢定。
- 新增 limit-up breadth PR>=80 的直接追價、日線回檔後 O2 可交易進場與非重疊事件比較。
- v6 discovery grid 保持不變；v7 新報酬期間只用於 robustness validation。

=======================================================

v8：Limit-up Cooldown Validation

- 固定基礎訊號為 limit_up_ratio rolling 252 日 PR>=80。
- 明確拆分隔日開高走低（O1→C1）、隔日收盤低於訊號日（C0→C1）與漲停廣度下降。
- 所有條件於 C1 後確認，正式進場固定為 O2，檢驗至 C2/C3/C5/C10。
- 主要對照為其他高漲停廣度但未符合相同冷靜條件的事件，避免等待價格較低造成機械性優勢。
- 同時輸出 raw 與 non-overlapping 事件、HAC、FDR、年度方向與樣本集中度。
- 新增同事件、相同出場日的 C1 診斷性進場與 O2 可交易進場比較；C1 不宣稱可直接成交。
